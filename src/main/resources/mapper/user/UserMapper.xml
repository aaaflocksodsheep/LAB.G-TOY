<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.labgenomics.practice.user.mapper.UserMapper">

	<select id="loadUserByUsername" parameterType="String" resultType="com.labgenomics.practice.user.model.UserInfo">
		SELECT
			user_id,
			login_id,
			email,
			password,
			role
		FROM User
		WHERE login_id = #{loginId}
	</select>

	<insert id="insertUser" parameterType="com.labgenomics.practice.user.model.User" useGeneratedKeys="true"
			keyProperty="userId" keyColumn="userId">
		insert into User (login_id, password, email, role, create_time)
		values (#{loginId}, #{password}, #{email}, 'USER', now());
	</insert>
</mapper>
